name: "Docker"
description: "Install Docker Engine, Docker CLI, and Docker Compose plugin"
category: "Services"

variables:
  docker_user:
    description: "User to add to the docker group (leave empty to skip)"
    default: ""
